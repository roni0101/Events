var App = (function () {
	
	// Init
	function init() {
		console.log('Init App');

		// Append events to layout
		// Check if user is loged in
		// Display data based on that 
		// Run app
	}

	function run() {
		console.log('app running...');
	}

	return {
		init:init
	}


})();